function SubCardPrice ({title, subtitle}) {
  return (
    <div style={{borderBottom: "1px solid #EAEAEA"}} className="py-4 text-sub_title20">
      <p className="font-calibri font-semibold text-sub_title20">{title}</p>
      <p className="font-calibri text-base">{subtitle}</p>
    </div>
  )
};

export default SubCardPrice;
