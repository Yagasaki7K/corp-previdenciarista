const SectionRow = ({children}) => {
  return (
    <section className="mb-[6px] font-calibri text-[13px] text-assine-preco opacity-60">
      {children}
    </section>
  )
};

export default SectionRow;
